<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Evolution">

        <activity android:name="com.thughan.evolution.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.thughan.kotlin.jetpack.compose.ComposeActivity"
            tools:node="remove" />

        <activity
            android:name="com.thughan.designmode.DesignActivity"
            tools:node="remove" />

        <activity
            android:name="com.thughan.ipc.aidl.AidlActivity"
            tools:node="remove" />

        <activity
            android:name="com.thughan.android.leakcanary.LeakCanaryActivity"
            tools:node="remove" />

    </application>

</manifest>
